{
  "name": "fallow",
  "version": "0.1.0",
  "private": true,
  "description": "Fallow — a prepaid, native-XLM compute marketplace for individual contributors on Stellar",
  "workspaces": [
    "shared",
    "backend",
    "contributor",
    "web",
    "example-buyer"
  ],
  "scripts": {
    "backend": "npm run dev -w backend",
    "contributor": "npm run dev -w contributor",
    "web": "npm run dev -w web",
    "client": "npm run start -w example-buyer",
    "keygen": "npm run keygen -w contributor",
    "typecheck": "tsc -b shared backend contributor example-buyer",
    "build:shared": "tsc -b shared",
    "build:backend": "tsc -b shared backend"
  },
  "engines": {
    "node": ">=22"
  }
}
